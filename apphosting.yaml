# Firebase App Hosting runtime config.
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  # Cost guards: scale to zero when idle, cap concurrency to avoid bill spikes.
  minInstances: 0
  maxInstances: 2
  concurrency: 80
  cpu: 1
  memoryMiB: 512
